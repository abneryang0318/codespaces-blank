{
    "name": "Java 17 (Stable)",

    // 【關鍵】告訴 Codespaces：
    // "請使用微軟官方的 Java 17 穩定映像檔，不要用你那個壞掉的 Blank"
    "image": "mcr.microsoft.com/devcontainers/java:17",

    "features": {
        // 我們順便連 Node.js 和 Python (yt-dlp) 都一起裝好
        "ghcr.io/devcontainers/features/node:1": {},
        "ghcr.io/devcontainers/features/python:1": {
            "version": "3.11"
        }
    },

    // 在容器「建立後」自動執行我們的安裝指令
    "postCreateCommand": "pip3 install yt-dlp && sudo apt-get update && sudo apt-get install -y openjdk-17-jdk"
}